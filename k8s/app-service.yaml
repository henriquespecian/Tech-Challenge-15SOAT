apiVersion: v1
kind: Service
metadata:
  labels:
    app: oficina-api
  name: oficina-api
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: oficina-api
  type: NodePort
status:
  loadBalancer: {}
