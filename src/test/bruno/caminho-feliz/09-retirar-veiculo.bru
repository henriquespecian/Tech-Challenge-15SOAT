meta {
  name: 09 - Retirar Veiculo
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/ordem-servico/:id/veiculo/retirar
  body: none
  auth: bearer
}

params:path {
  id: {{ordemServicoId}}
}

auth:bearer {
  token: {{token}}
}
