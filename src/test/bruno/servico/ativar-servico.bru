meta {
  name: Ativar Servico
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/servico/:id/ativar
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

params:path {
  id: {{servicoId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
